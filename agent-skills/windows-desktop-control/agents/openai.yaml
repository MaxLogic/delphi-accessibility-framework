interface:
  display_name: "Windows Desktop Control"
  short_description: "Background control or leased real input"
  default_prompt: "Use pseudo-headless Background Command Mode by default when the app exposes a compatible bridge, including exact selection and named action or menu invocation. Choose announced, leased Foreground Input Mode only for real mouse, keyboard, foreground-dependent, or screen-reader behavior; never silently fall back to it."
