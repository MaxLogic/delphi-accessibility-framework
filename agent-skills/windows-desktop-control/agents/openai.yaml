interface:
  display_name: "Windows Desktop Control"
  short_description: "Control desktop apps safely"
  default_prompt: "Use Background Command Mode by default for routine testing when the app exposes a compatible bridge. Choose announced, leased Foreground Input Mode only for real mouse, keyboard, foreground-dependent, or screen-reader behavior; never silently fall back to it."
